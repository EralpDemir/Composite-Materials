function [K, F] = FEM_initialize(numind)
%FEM_initialize takes the element type and mesh nodal information and
%initialized the FEA matrices related to each element type


    
        K = zeros(numind);
        F = zeros(numind,1);

        
        
    
        
        
end

